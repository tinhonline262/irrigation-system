name: CI/CD Pipeline

on:
  pull_request:
    branches: [main]

  push:
    branches: [main]

jobs:
  deploy:
    if: github.ref_name == 'main' && github.event_name == 'push'
    uses: ./.github/workflows/deploy.yml
    with:
      environment: stagingmain
    secrets: inherit
